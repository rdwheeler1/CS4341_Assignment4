public class Agent {
    private int xCoord;
    private int yCoord;

    // TODO: Possibly move function to Main(?)
    // TODO: Add Action as a parameter
    public void qFunction(Coordinate state){
        // TODO: Add Q(s, a) equation
        // New Q(s, a) = Current Q(s, a) + alpha(Reward + gamma * max(Q(s', a')) - Q(s, a))

    }
}
